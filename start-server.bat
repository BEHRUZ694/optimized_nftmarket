@echo off
echo Starting NFT Marketplace Server...
echo Server will be available at: http://localhost:8080
echo Press Ctrl+C to stop the server
echo.

cd /d "%~dp0"
powershell -Command "& { $port = 8080; $path = Get-Location; $listener = New-Object System.Net.HttpListener; $listener.Prefixes.Add('http://localhost:$port/'); $listener.Start(); Write-Host 'Server running at http://localhost:$port/'; Write-Host 'Press Ctrl+C to stop'; try { while ($listener.IsListening) { $context = $listener.GetContext(); $request = $context.Request; $response = $context.Response; $localPath = $request.Url.LocalPath; if ($localPath -eq '/') { $localPath = '/index.html' }; $filePath = Join-Path $path $localPath.TrimStart('/'); if (Test-Path $filePath -PathType Leaf) { $content = Get-Content $filePath -Raw -Encoding UTF8; $response.ContentType = if ($filePath.EndsWith('.html')) { 'text/html' } elseif ($filePath.EndsWith('.css')) { 'text/css' } elseif ($filePath.EndsWith('.js')) { 'application/javascript' } elseif ($filePath.EndsWith('.json')) { 'application/json' } else { 'text/plain' }; $response.StatusCode = 200; $buffer = [System.Text.Encoding]::UTF8.GetBytes($content); $response.ContentLength64 = $buffer.Length; $response.OutputStream.Write($buffer, 0, $buffer.Length) } else { $response.StatusCode = 404; $notFound = '<h1>404 - File Not Found</h1><p>$filePath</p>'; $buffer = [System.Text.Encoding]::UTF8.GetBytes($notFound); $response.ContentLength64 = $buffer.Length; $response.OutputStream.Write($buffer, 0, $buffer.Length) }; $response.OutputStream.Close() } } finally { $listener.Stop(); $listener.Close() } }"
